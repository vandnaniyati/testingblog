<h1> Home page </h1>

<%= link_to "NewAricle", new_article_path%></br>

<div class="card">
  <div class="card-header">
     Article
  </div>
  <div class="card-body">
    <% @articles.each do |article| %>
      <h5 class="card-title"><%= link_to article.title, article %></h5>
      <p class="card-text"><%= link_to article.body, article %></p>
    <%end%>
    <a href="#" class="btn btn-primary">Go somewhere</a>
  </div>
</div>