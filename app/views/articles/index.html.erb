<div class="mt-2">
  <div class="col-lg-6 mx-auto">
    <h1>Article</h1>
    <% @articles.each do |article| %>
      <div class="card shadow">
          <div class="card-body">
            <%= link_to article.title, article %>
            <p class="card-text"><%= link_to "show", article_path(article)%></p>
          </div>
      </div>
    <%end%>
  <%= link_to "NewAricle", new_article_path, class:"btn btn-outline-primary"%></br>
  </div>
</div>